@model YSA.Web.Models.ViewModels.ArtistaDetallesViewModel
@{
    // Usa el nombre del artista como título de la página
    ViewData["Title"] = $"Perfil de {Model.NombreCompleto}";
}

<style>
    /* ------------------------------------- */
    /* 1. Estilos Base */
    /* ------------------------------------- */

    /* Estilo para la imagen de perfil del artista */
    .perfil-img {
        width: 200px;
        height: 200px;
        object-fit: cover;
        border: 5px solid #fff;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    /* Estilo para las imágenes del portafolio */
    .portafolio-img {
        height: 300px; /* Altura fija para uniformidad */
        width: 100%; /* Asegura que ocupe todo el ancho de su contenedor */
        object-fit: cover;
    }

    .card-portafolio {
        cursor: pointer; /* Indica que es clickeable, aunque la lógica no esté en esta vista */
    }

        .card-portafolio:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15) !important;
            transition: all 0.3s ease;
        }

    .biografia-text {
        white-space: pre-wrap; /* Mantiene saltos de línea y espacios del texto */
        font-size: 1.1rem;
        line-height: 1.8;
        color: #555;
    }

    @@media (max-width: 767.98px) {
        /* Encabezado: Reducir tamaño de imagen y asegurar centrado */
        .perfil-img {
            width: 150px;
            height: 150px;
            /* El text-center del padre ya centra la imagen en móvil */
        }
        /* Encabezado: Reducir el tamaño del título principal para que no sea abrumador */
        .display-4 {
            font-size: 2.5rem;
        }
        /* Portafolio: Reducir la altura de las imágenes para que no ocupen toda la pantalla verticalmente */
        .portafolio-img {
            height: 200px;
        }
    }
</style>

<div class="container py-5 mb-5 pb-5">

    <div class="row mb-5 align-items-center bg-light p-4 rounded-3 shadow-sm">

        <div class="col-12 col-md-3 text-center">
            @if (!string.IsNullOrEmpty(Model.UrlFotoPerfil))
            {
                <img src="@Model.UrlFotoPerfil" alt="Foto de Perfil" class="img-fluid rounded-circle perfil-img">
            }
            else
            {
                <i class="fas fa-user-circle fa-8x text-secondary"></i>
            }
        </div>

        <div class="col-12 col-md-9 mt-3 mt-md-0 text-center text-md-start">
            <h1 class="display-4 fw-bold mb-0">@Model.NombreCompleto</h1>
            <p class="lead text-primary fw-semibold">Artista en YoSoyArte</p>
        </div>
    </div>

    <hr class="mb-5">

    <div class="row mb-5">
        <div class="col-12">
            <h2 class="h3 mb-4 fw-bold border-bottom pb-2">Biografía</h2>
            @if (!string.IsNullOrWhiteSpace(Model.Biografia))
            {
                <p class="biografia-text">@Html.Raw(Model.Biografia)</p>
            }
            else
            {
                <p class="text-muted fst-italic">Este artista aún no ha compartido su biografía detallada.</p>
            }
        </div>
    </div>

    @if (Model.Cursos != null && Model.Cursos.Any())
    {
        <div class="row mt-5 pb-5">
            <div class="col-12">
                <h2>Cursos Impartidos por @Model.NombreCompleto</h2>
                <hr />
            </div>
        </div>

        <div class="row g-4 mb-5">
            @foreach (var curso in Model.Cursos)
            {
                <div class="col-lg-4 col-md-6 col-sm-12">
                    <div class="card h-100 shadow-sm">
                        <img src="@Url.Content(curso.UrlImagen)" class="card-img-top" alt="Portada de @curso.Titulo">
                        <div class="card-body">
                            <h5 class="card-title">@curso.Titulo</h5>
                            <p class="card-text text-muted">@Html.Raw(curso.DescripcionCorta)</p>
                            <p class="fw-bold">
                                @curso.Precio.ToString("C", new System.Globalization.CultureInfo("es-VE"))
                            </p>
                            <a href="@Url.Action("Detalles", "Curso", new { id = curso.Id })" class="btn btn-primary w-100">Ver Curso</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    }

</div>